.index-campaign.horizontal-padding-15
  .index-campaign__left
    %p.index-campaign__left-top
      スマホでカンタン
      %br
      フリマアプリ
    %p.index-campaign__left-bottom
      メルカリはスマホカメラからすぐに出品できる
      %br
      購入時も安心な独自システムのアプリです
    = image_tag 'app-store.svg'
    = image_tag 'google-play.svg'
  = image_tag 'campaign_bg_phone.png', class: "index-campaign__phone"
.pickups-header
  人気のカテゴリー
  .pickups-header__category-names
    - @category_ranking&.each do |category|
      = link_to category_path(category) do
        .pickups-header__category-names--category-name
          = category.name
.pickups.horizontal-padding-15
  %br
  - @sorted_items&.each do |items|
    .pickup
      %span.pickup__name
        = items[0].category.root.name
        新着アイテム
      = link_to category_path(items[0].category) do
        %span.pickup__display-all
          もっと見る >
      .pickup__items
        = render items
